<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coffehouse">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoffeHouse">
        <activity android:name=".dialog_noti"></activity>
        <activity android:name=".Noti" />
        <activity android:name=".rewards" />
        <activity android:name=".TichDiem" />
        <activity android:name=".Coupon" />
        <activity android:name=".Detail_ChiTietUuDai" />
        <activity android:name=".edit_profile" />
        <activity android:name=".profile" />
        <activity android:name=".CaiDat" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>